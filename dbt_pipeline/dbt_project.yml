name: dbt_pipeline
version: 1.0.0
config-version: 2
profile: dbt_pipeline
vars:
  surrogate_key_treat_nulls_as_empty_strings: true
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
models:
  dbt_pipeline:
    +materialized: view
